pragma solidity >= 0.8.9 <= 0.8.9;

contract BillCollectionContract {
    string public amount;

    function addBillAmount(string memory d) public {
        amount = d;	
    }

    function getBillAmount() public view returns (string memory) {
        return amount;
    }

    constructor() public {
        amount = "empty";
    }
}