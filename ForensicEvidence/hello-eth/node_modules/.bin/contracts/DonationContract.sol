pragma solidity >= 0.8.9 <= 0.8.9;

contract DonationContract {
    string public donation;

    function setDonation(string memory donate) public {
        donation = donate;	
    }

    function getDonation() public view returns (string memory) {
        return donation;
    }

    constructor() public {
        donation = "empty";
    }
}